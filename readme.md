Tests for comparing Mongoose ObjectId for different Mongoose versions.

Mongoose v5
```bash
cd mongoosev5
nvm use 16
npm i
./node_modules/.bin/mocha test.js
```

Mongoose v6

```bash
cd mongoosev6
nvm use 16
npm i
./node_modules/.bin/mocha test.js
```